 <mvc:View controllerName="pinaki.app.credentialManager.controller.MainView"
  displayBlock="true"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc">
  <App id="idAppControl" >
    <pages>
      <Page title="{i18n>title}" backgroundDesign="Solid">
        <Toolbar design="Transparent" style="Clear"> 
          <ToolbarSpacer/>
          <Button type="Transparent" icon="sap-icon://download" press="downloadCSV" tooltip="Download"/>
          <Button type="Transparent" icon="sap-icon://refresh" press="reload" tooltip="Refresh"/>
          <Button type="Transparent" icon="sap-icon://lightbulb" press="changeTheme" tooltip="Toggle Dark mode"/>
        </Toolbar>
         <List class="sapUiSmallMargin sapUiForceWidthAuto sapUiSizeCompact" 
          backgroundDesign="Translucent" id="credList"
          showSeparators="All"
          items="{
            path:'/aCreds',
            sorter : {
              path : 'lastChanged',
              descending  : true
            }
          }"
         >
          <headerToolbar>
            <OverflowToolbar>
              <Title text="My Credentials " titleStyle="H3"/>
              <ToolbarSpacer/>
              <SearchField width="40%" liveChange="onCredSearch"/>
              <Button type="Accept" icon="sap-icon://add" press="onAddCredentialpress"/>
            </OverflowToolbar>
          </headerToolbar>
          <items>
            <CustomListItem class="sapUiSmallMarginBottom" type="Active" press="toggleEditMode">
              <HBox justifyContent="SpaceBetween" class="sapUiTinyMargin">
                <VBox width="70%">
                  <Label text="{name}" visible="{=!${editMode}}"/>
                  <Input value="{name}" placeholder="Credential Name" visible="{editMode}"/>

                  <Input value="{username}" placeholder="User Name" visible="{editMode}"/>
                  <Input value="{password}" placeholder="Credential" visible="{editMode}"/>
                  <Text  text="{username} | ***********" visible="{=!${editMode}}"/> 
                </VBox>
                <HBox>
                  <Button type="Reject" icon="sap-icon://delete" press="onDeleteCredPress"/>
                </HBox>
              </HBox>
            </CustomListItem>
          </items>
         </List>
         <footer>
           <Toolbar design="Info" style="Clear">
             <ToolbarSpacer/> 
             <Text text= " © Pinaki Patra 2021"/>
             <ToolbarSpacer/> 
           </Toolbar>
         </footer>
      </Page>
    </pages>
  </App>
</mvc:View>